apiVersion: v1
kind: Service
metadata:
  name: {{ include "afk.fullname" . }}
  labels:
    {{- include "afk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "afk.selectorLabels" . | nindent 4 }}
